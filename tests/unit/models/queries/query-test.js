import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('model:queries/query', 'Query', {
  // Specify the other units that are required for this test.

});


test('it exists', function(assert) {
  var model = this.subject();
  // var store = this.store();
  assert.ok(!!model);
});
